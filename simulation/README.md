# Drift Simulation

`hindcast/` estimates probable spill origin using historical wind and current fields.

`forecast/` projects the slick envelope forward with ensemble uncertainty.

Use an established transport engine such as OpenDrift instead of a hand-rolled physical model.
